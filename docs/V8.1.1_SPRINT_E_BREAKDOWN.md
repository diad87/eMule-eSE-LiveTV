# v8.1.1 Sprint E — Desglose ejecutable (Data plane bulk por túnel, sin sacrificar bitrate)

> Spec de implementación del **Sprint E re-especificado** según la decisión §12.4 de
> [`V8.1_SPRINT_PLAN.md`](V8.1_SPRINT_PLAN.md) y el análisis de
> [`V8.1.1_SPRINT_E_SPEED.md`](V8.1.1_SPRINT_E_SPEED.md): los chunks de vídeo viajan por
> el túnel de 2 saltos a **bitrate nativo**, con overhead objetivo **~10-15 %** en el path
> del viewer (FEC + padding), en vez del cap fijo de 600-800 kbps del plan original.
> **Cierra G1**: en modo `Máxima privacidad` ningún peer que sirve contenido ve la IP del viewer.
>
> Refs `archivo:línea` verificadas por exploración directa del código a 2026-06-11.
> **Fases:** E-α (= release v8.1.1) y E-β (= release v8.1.2, §8 de este doc).
> Depende de **Sprint A completo** (commit `2c9bfeb`) y de las interfaces de **Sprint C**
> (C2 subscribe proxied, C5 handoff, C7 exit-multicast) — ver §0.3.

---

## 0. Pre-flight

### 0.1 Lo que YA existe (NO reconstruir)

| Pieza | Dónde | Estado |
|---|---|---|
| Celda 512 B + framing multi-cell (sub-header 7 B + frag-header 8 B) | `LiveCellQueue.h:25-27, 69-71` | Funciona (Sprint A). Límites: `TUN_MSG_MAX_BYTES` 64 KB (`:74`), `TUN_MSG_MAX_FRAGS` 256 (`:109`). |
| Dispatcher genérico exit-side | `LiveTunnel.h:126-143` `RegisterExitHandler` | Funciona (A2). |
| Operaciones diferidas en el exit (A6) | `LiveTunnel.h:335-360` `Begin/Complete/AbortExitOperation` | Funciona; sweep TTL en `Tick()`. |
| Pinning de mensaje a circuito + retry (A4) | `LiveTunnel.h:398-428` `SendMessagePinned` | Funciona. |
| Pool de circuitos + rotación | `LiveTunnel.h:30-34` | 3-5 circuitos; rotación hoy a 5 min — **valor temporal de test** que contradice la Decisión 5.3 de la tesis (30 s, parámetro de seguridad); restaurar antes del data plane. E2.3 fija la cadencia bulk. |
| `TunnelEndpoint` opaco | `LiveTunnel.h:42-45` | `{circ_id, remote_pubkey[32]}`. |
| Subscribe tunelizado (C1/C2) | `LiveTunnel.h:178-180` `TunneledLiveSubscribe`, `:380` `ExitHandle_LiveSubscribe` | En el árbol (Sprint C en curso). |
| Identidad durable de peer del mesh (C6) | `LiveProtocol.h:136-141` `LivePeerId` (`LPID_PUBKEY`) | En el árbol. |
| Chunk firmado | `opcodes.h:307` `OP_LIVE_CHUNK_V2`: StreamHash 16 + SeqNum 4 + Timestamp 4 + DataSize 4 + Bitrate 2 + **sha256 32 + Sig Ed25519 64** + Data | El camino de verificación contra la pubkey pinada ya existe (`LiveStreamManager.h:364`). **La reconstrucción FEC debe entregar este registro íntegro** para reusarlo sin tocar nada. |
| Ring de segmentos | `LiveProtocol.h:12-15` | 16 segmentos × 2 s; bitmap `uint16` en `OP_LIVE_HEARTBEAT` (`opcodes.h:302`). **No tocar** (compat). |
| Gate de playback | `LiveStreamManager.cpp` `ESE_HLS_MIN_BUFFER_SEGMENTS = 2` | Startup-only gate. |
| Caps eSE | `opcodes.h:507-517` | Último bit usado: `ESE_CAP_TUNNEL_DATAPLANE 0x1000`. **Siguiente libre: `0x2000`.** |
| libutp vendorizado | submódulo `libutp/` + wrapper `eMuleAI/UtpSocket.cpp` | Disponible para E10 (E-β, condicional). |

### 0.2 Restricción de opcodes descubierta (corrige al plan)

El plan (§4-E1, §7) asumía "opcode nuevo del rango reservado `0xD0-0xDF`". **El rango está
completo** (`opcodes.h:314-334`). Pero `OP_LIVE_T_CHUNK 0xD9` está reservado desde F0
exactamente para "tunneled CHUNK_V2 wrapper" y su handler es un stub log-and-drop en todos
los forks desplegados (v8.0.x). **Decisión E1:** implementar `0xD9` como el carrier bulk
(renombrado `OP_LIVE_BULK_CELL`, alias documentado), y cerrar C4 así:

- `0xD9` → **se implementa** (carrier de celdas bulk, §2.1).
- `0xD6, 0xD7, 0xD8, 0xDA-0xDD` → **se retiran como sustituidos**: el plano de control
  tunelizado viaja como sub-protocolo `TUN_OP_*` dentro de `CELL_RELAY` (decisión ya
  implícita en C1/C2). Documentar en `opcodes.h` y retirar el stub `[STUB F0]`
  (`ListenSocket.cpp:2142-2153`) para los retirados, manteniendo el silencio ante peers viejos.

### 0.3 Dependencias de Sprint C (interfaces que E consume)

| Interfaz | De | Estado a 2026-06-11 | Si no está al empezar E |
|---|---|---|---|
| Subscribe proxied: el exit reenvía `OP_LIVE_SUBSCRIBE` al broadcaster en su propio nombre | C2 | En el árbol (`ExitHandle_LiveSubscribe`) | Bloqueante — E no arranca sin C2 |
| **Recepción headless de chunks en el exit** para streams proxied (el exit recibe `OP_LIVE_CHUNK_V2`/heartbeats del broadcaster sin ser "viewer" de UI) | C2/C7 | Verificar al arrancar E | Si C7 no lo entregó, lo añade **E3.1** |
| Suscripción única upstream por streamKey con N `TunnelEndpoint` detrás (proxy multicast) | C7 | Planificado | E-α funciona sin él (1 sub upstream por viewer) pero E3.2 lo asume — coordinar |
| Handoff control→datos según modo | C5 | Planificado | E-α lo cierra: en `Máxima privacidad` el handoff es "bulk tunnel", nunca directo |

---

## 1. Arquitectura del data plane (E-α)

```
 BROADCASTER                EXIT (proxy del viewer)                  HOP1            VIEWER
 ───────────                ───────────────────────                  ────            ──────
 OP_LIVE_CHUNK_V2  ──────▶  pull loop headless (E3.1)
 (directo, mesh:             │ registro CHUNK_V2 íntegro (firmado)
  el broadcaster ve          ▼
  la IP del EXIT)           E8: pad → FEC RS(k,r) → k+r símbolos de 16 KB
                             │
                            E3.3: scheduler de símbolos
                             │   (stripe entre los circuitos bulk del viewer,
                             │    créditos E7 por circuito)
                             ▼
                            OP_LIVE_BULK_CELL (0xD9) ─────────▶ forward ─────▶ reassembly E3.4
                            cifrado onion por capa (E1)         por circ_id    cualquier k de k+r
                                                                               → CHUNK_V2 íntegro
                                                                               → verificación sha256+Ed25519
                                                                                 EXISTENTE → LiveChunkBuffer
                                                                                 → HLS local → VLC

 Control (celdas 512 B clásicas, ya existentes): TUN_OP_BULK_* (subscribe bulk, NACK,
 feedback) por el MISMO sub-protocolo de Sprint A. Créditos: BULK_ACK dentro del canal bulk.
```

Principios:
- **Push, no pull, en el tramo tunelizado:** tras el subscribe bulk, el exit empuja el
  live-edge según le llegan los segmentos. El viewer solo habla para gap-fill (NACK) y
  feedback. Elimina 1 RTT de túnel por segmento.
- **El registro `CHUNK_V2` viaja íntegro** (cabecera + sha256 + firma + data, padded) como
  payload del FEC: la reconstrucción entrega exactamente lo que el camino directo entrega,
  y la verificación/inyección del viewer **no cambia ni una línea** aguas abajo.
- **1 símbolo FEC = 1 celda bulk.** Sin fragmentación adicional: el símbolo de 16 KB es la
  unidad de transporte, retransmisión (NACK) y striping.

---

## 2. Wire format nuevo

### 2.1 `OP_LIVE_BULK_CELL` (0xD9) — celda bulk variable, por enlace

```
[0..3]   circ_id     uint32 LE    (mismo espacio de ids que las celdas 512 B)
[4]      bcmd        uint8        0x01 BULK_DATA · 0x02 BULK_ACK (créditos E7)
[5..8]   length      uint32 LE    bytes de payload que siguen
[9..]    payload     cifrado (≤ BULK_CELL_MAX_PAYLOAD)
```

- Viaja como packet eMule normal (protocolo de extensión); cada hop lo reenvía por el
  estado de forwarding del `circ_id` **igual que `OP_LIVE_TUNNEL_CELL`**, pero sin
  repack a 512 B.
- **Cripto E-α:** AEAD onion por capa (ChaCha20-Poly1305, una capa por hop, nonce por
  dirección — el mismo esquema de `LiveCircuit.cpp`) sobre el payload completo. A 16 KB
  por celda son ~32 operaciones AEAD por segmento y dirección (vs ~1083 con celdas de
  512 B) — S-OPT-14 cumplida. El split payload-e2e/MAC-por-hop es E9 (E-β).
- **Sin padding por celda:** tras E8 todos los símbolos miden exactamente
  `BULK_SYMBOL_BYTES` — tamaño uniforme de serie, mejor que padding aleatorio.

### 2.2 `BULK_DATA` — payload (tras descifrar)

```
[0..15]  stream_key   16 B
[16..19] seq          uint32 LE   nº de segmento
[20]     symbol_idx   uint8       0..k+r-1 (índices ≥ k = paridad)
[21]     k            uint8       símbolos de datos del segmento
[22]     r            uint8       símbolos de paridad
[23]     flags        uint8       reservado (0)
[24..27] msg_len      uint32 LE   bytes reales del CHUNK_V2 serializado (pre-padding)
[28..]   symbol       BULK_SYMBOL_BYTES exactos
```

### 2.3 `BULK_ACK` — payload (créditos, E7)

```
[0..15]  stream_key   16 B
[16..19] credits      uint32 LE   símbolos adicionales que el receptor acepta
[20..23] highest_seq  uint32 LE   mayor seq completamente reconstruido (telemetría E4′)
```

### 2.4 `TUN_OP_*` nuevos (control, celdas 512 B clásicas; rango `0x50-0x5F` reservado para E)

| Valor | Op | Dirección | Body |
|---|---|---|---|
| `0x50` | `TUN_OP_BULK_SUBSCRIBE` | V → exit | stream_key 16 + flags 1 + r_pref 1 — activa el push bulk sobre una sesión C1/C2 ya establecida |
| `0x51` | `TUN_OP_BULK_PARAMS` | exit → V | stream_key 16 + symbol_bytes 4 + k_max 1 + r 1 + window 2 — confirma parámetros y ventana inicial |
| `0x52` | `TUN_OP_BULK_NACK` | V → exit | stream_key 16 + count 1 + (seq 4 + symbol_mask 8)×count — gap-fill: re-push de símbolos/segmentos perdidos |
| `0x53` | `TUN_OP_BULK_FEEDBACK` | V → exit | stream_key 16 + goodput_kbps 4 + loss_pct 1 + buffer_depth 1 — alimenta E4′ (adaptación de r y rebalanceo) |
| `0x54` | `TUN_OP_BULK_UNSUB` | V → exit | stream_key 16 — corta el push (también implícito al morir la sesión) |

### 2.5 Constantes nuevas (`LiveCellQueue.h`)

```
BULK_SYMBOL_BYTES        = 16384   // 1 símbolo FEC = 1 celda bulk
BULK_CELL_MAX_PAYLOAD    = 17408   // símbolo + cabecera 2.2 + AEAD (16 B/capa × 2)
BULK_MAX_K               = 64      // segmento ≤ 1 MB (≈4 Mbit/s con segmentos de 2 s)
BULK_MAX_R               = 8
BULK_DEFAULT_R           = 3       // ~9.4 % de paridad con k=32; adaptativo 2..5 (E4′)
BULK_WINDOW_SYMBOLS      = 32      // créditos iniciales por circuito (≈512 KB en vuelo)
BULK_SUB_TTL_MS          = 30000   // sub bulk sin heartbeat de sesión → se barre
```

### 2.6 Capability

`ESE_CAP_TUNNEL_BULK = 0x00002000` (bit 13, siguiente libre tras `0x1000`, `opcodes.h:517`).
El path bulk solo se usa si **el exit Y todos los hops** del circuito anuncian el bit.

**Fallback sin el bit** (peer v8.0.0/v8.1 en el camino): cada símbolo viaja como mensaje
multi-cell clásico `TUN_OP` de Sprint A (16 KB = 36 celdas de 458 B — cabe de sobra en
`TUN_MSG_MAX_BYTES`). Funciona, ~3× más overhead de CPU/cabeceras; la telemetría E4′ lo
detecta y la política de fallback (D3) decide. **Nunca** se cae a chunks directos en modo
`Máxima privacidad` con política STRICT.

---

## 3. Desglose por tarea (E-α = v8.1.1)

Tamaño: **S**≈½d · **M**≈1-2d · **L**≈3-5d. `Test:` = cómo verificar.

### E1 — Carrier bulk + forwarding + cripto — L
**Archivos:** `opcodes.h`, `ListenSocket.cpp`, `LiveTunnel.cpp/.h`, `LiveCellQueue.h`, `LiveCircuit.cpp`.

| # | Sub-tarea | Tam |
|---|---|---|
| E1.1 | Renombrar `OP_LIVE_T_CHUNK`→`OP_LIVE_BULK_CELL` (alias comentado); retirar stubs sustituidos `0xD6-0xD8, 0xDA-0xDD` (§0.2, cierra C4). | S |
| E1.2 | Parser/builder de celda bulk (§2.1) + handler en `ListenSocket` → `CLiveTunnel::OnBulkCellReceived(circ_id, bcmd, payload, len, fromPeer)`. | M |
| E1.3 | Forwarding relay-side: lookup del `circ_id` en el estado de circuitos existente; reenvío sin repack. Mismo camino de `OnCellReceived` pero sin el límite de 512 B. | M |
| E1.4 | Cripto onion sobre payload bulk: reusar `OnionEncrypt`/`OnionDecryptAll` (`LiveCircuit.cpp:49-126`) parametrizando el tamaño (hoy asumen ≤512 B; aplicar S-OPT-1: scratch en pila → heap acotado para 17 KB). Nonce por dirección como hoy. | M |
| E1.5 | Bit `ESE_CAP_TUNNEL_BULK 0x2000` + negociación: el originador conoce las caps de cada hop (A7); marcar cada circuito como bulk-capable solo si todos sus hops lo son. | S |
| E1.6 | Fallback per-symbol multi-cell (§2.6) cuando el circuito no es bulk-capable. | M |

**Test E1:** echo bulk loopback (op de test estilo A1.7 con celda bulk de 16 KB por un
circuito de 2 saltos); verificar AEAD por capa, forwarding, y fallback multi-cell forzando
caps=0 en un hop.

### E2 — Pool de circuitos bulk por stream (absorbe E6 pre-warm) — M
**Archivos:** `LiveTunnel.cpp/.h`, `LiveStreamManager.cpp`, `KadV2TunnelPool.cpp`.

| # | Sub-tarea | Tam |
|---|---|---|
| E2.1 | `BulkCircuitSet` por stream activo: 2-4 circuitos bulk-capable, **disjuntos en hop1 si hay candidatos suficientes** (diversidad de path). | M |
| E2.2 | Sin cover traffic en circuitos bulk (S-OPT-19) — el flujo es la cobertura. | S |
| E2.3 | Rotación **en frontera de segmento**: drain de símbolos en vuelo → swap al circuito sucesor pre-construido → el segmento siguiente ya va por el nuevo. Cadencia objetivo ~30 s (Decisión 5.3 de la tesis; el 5 min actual de `LiveTunnel.h:32` es temporal de test) = rotar cada ~15 segmentos de 2 s. El drain es obligatorio siempre. | M |
| E2.4 | Pre-warm (era E6): disparar la construcción del `BulkCircuitSet` en `JoinStream`, antes del primer push — en paralelo con el subscribe C1/C2, no después. | S |

**Test E2:** `JoinStream` con cronómetro: circuitos Active antes de que llegue el `SUB_ACK`;
matar un circuito mid-stream → el sucesor entra sin stall (con E8, sin pérdida visible).

### E3 — Pipeline push exit→viewer + scheduler de símbolos — L
**Archivos:** `LiveTunnel.cpp/.h`, `LiveStreamManager.cpp/.h`.

| # | Sub-tarea | Tam |
|---|---|---|
| E3.1 | **Ingest headless en el exit** (si C7 no lo entregó, §0.3): por cada suscripción proxied, el exit corre un pull-loop mínimo contra el broadcaster (heartbeat → bitmap → `OP_LIVE_REQUEST` del live-edge) y recibe `OP_LIVE_CHUNK_V2` **sin** instanciar el camino de viewer/UI/HLS. Estado: `ProxiedStream{streamKey, upstream peer, último seq, subs bulk}`. | L |
| E3.2 | Registro de subs bulk en el exit: `m_bulkSubs: streamKey → vector<BulkSub{weak_ptr<CLiveCircuit>, circ_id, créditos, r, lastAck}>`. Alta por `TUN_OP_BULK_SUBSCRIBE` (sobre la sesión C1/C2), baja por `BULK_UNSUB`/muerte de circuito/TTL. Con C7: **una** suscripción upstream sirve a N subs. | M |
| E3.3 | **Scheduler de símbolos:** al completar E8 la codificación de un segmento, repartir los k+r símbolos round-robin entre los circuitos del `BulkCircuitSet` del sub, respetando créditos E7. Pin de símbolo a circuito (un símbolo no se trocea). Los símbolos de paridad van por circuitos distintos a los de datos cuando hay ≥2. | M |
| E3.4 | **Reassembly+decode en el viewer:** buffer por (streamKey, seq); con ≥k símbolos → decode RS → `CHUNK_V2` íntegro → **inyectar en el handler existente** de `OP_LIVE_CHUNK_V2` (verificación sha256+Ed25519 contra pubkey pinada, `LiveChunkBuffer`, HLS) sin duplicar lógica. TTL de buffers parciales (sweep estilo A5). | M |
| E3.5 | **Gap-fill:** si un seq no se completa en `ESE_LIVE_REQUEST_TIMEOUT`×2, `TUN_OP_BULK_NACK` con la máscara de símbolos faltantes; el exit re-empuja solo esos (los retiene `ESE_LIVE_MAX_SEGMENTS`×2 s). | M |

**Test E3:** 2 PCs reales: broadcaster en PC1, viewer tunelizado en PC2 (exit=PC1
self-loopback como en v8.0.0): VLC reproduce el stream end-to-end por bulk cells;
verificar con el log que **ningún** `OP_LIVE_CHUNK_V2` directo llegó al viewer.

### E4′ — Entrega adaptativa medida (SUSTITUYE al "bitrate adaptativo 600-800") — M
**Archivos:** `LiveTunnel.cpp`, `LiveStreamManager.cpp`, `KadV2ModeSelector.cpp`.

| # | Sub-tarea | Tam |
|---|---|---|
| E4′.1 | Telemetría por circuito bulk: EWMA de goodput (de los BULK_ACK), tasa de símbolos perdidos (de los NACK), profundidad del buffer de reassembly. Extiende S-OPT-24. | M |
| E4′.2 | **Adaptación de r** (no del bitrate): pérdida sostenida <1 % → r=2; 1-5 % → r=3; >5 % → r=5 + rebalanceo de stripes hacia los circuitos sanos. El bitrate de la fuente **no se toca**. | M |
| E4′.3 | Shortfall real (goodput agregado < bitrate×1,15 sostenido 10 s): aplicar la política D3 — STRICT avisa y mantiene (buffer crece); BALANCED sube el gate E5; BEST_EFFORT ofrece caída a Directo **con aviso explícito de pérdida de privacidad**. Sin constante 600-800 en ningún sitio. | M |
| E4′.4 | Telemetría en `/api/live/privacy` + `NetworkInfoDlg` (extiende D8): goodput túnel, r actual, % pérdida, segmentos reconstruidos vía paridad. **Endpoint marshaled al main thread** (regla `/api/live`). | S |

**Test E4′:** estrangular el upload de un hop (limitador externo) → r sube, stripes se
rebalancean, el vídeo no se detiene; quitar el límite → r baja a 2. Sin cambios de bitrate.

### E5 — Gate de playback condicional — S
Con E8 activo el gate se queda en 2 segmentos (el FEC absorbe el jitter); subir a 3 **solo**
en el fallback per-symbol multi-cell (E1.6) o mientras r esté en 5 (churn alto). Era "2→4-6
fijo en modo tunelizado"; ya no.

**Test E5:** TTFF medido en modo bulk ≈ TTFF directo + build de circuito (<8 s con E2.4).

### E7 — Backpressure por créditos — M
**Archivos:** `LiveTunnel.cpp/.h`.

| # | Sub-tarea | Tam |
|---|---|---|
| E7.1 | Ventana de créditos por (sub, circuito): inicial `BULK_WINDOW_SYMBOLS`; el viewer concede más vía `BULK_ACK` según consume. Dimensionada > BDP (32×16 KB = 512 KB ≈ 2 s de stream a 2 Mbit/s). **No** stop-and-wait. | M |
| E7.2 | Sensibilidad a la cola local: si la cola de envío del socket del hop supera un umbral, pausar el push de ese circuito y rebalancear al resto (no acumular RAM). | S |
| E7.3 | Sweep: subs sin ACK en `BULK_SUB_TTL_MS` → baja + liberación de buffers (patrón A5). | S |

**Test E7:** viewer artificialmente lento (no concede créditos) → el exit deja de empujar a
ese sub sin afectar a otros; RAM del exit plana.

### E8 — FEC Reed-Solomon + serialización + padding (absorbe E13) — L
**Archivos:** nuevos `LiveFec.cpp/.h`; `LiveTunnel.cpp`, `LiveStreamManager.cpp`.

| # | Sub-tarea | Tam |
|---|---|---|
| E8.1 | **RS sistemático sobre GF(256)** (matriz Cauchy/Vandermonde, k≤64, r≤8): `FecEncode(data, k, r) → símbolos`, `FecDecode(símbolos≥k) → data`. Autocontenido (~300 líneas, tablas log/exp), **sin dependencia nueva**. Los k primeros símbolos son los datos tal cual → sin pérdida llegan k símbolos de datos y el decode es un memcpy. | L |
| E8.2 | Serialización: registro `OP_LIVE_CHUNK_V2` completo → padding aleatorio hasta múltiplo de `BULK_SYMBOL_BYTES` (`msg_len` real en la cabecera §2.2, cifrada) → split en k símbolos → encode r de paridad. | M |
| E8.3 | **Padding cuantizado (era E13):** la cuantización a múltiplos de 16 KB sale gratis de E8.2 (~1-2 % de overhead medio sobre segmentos de ~500 KB) y uniformiza todas las celdas bulk → debilita el fingerprint por tamaño frente a hop1 y observadores. | S |
| E8.4 | Unit tests deterministas: encode/decode con 0..r símbolos perdidos en posiciones aleatorias, k=1..64; round-trip de un CHUNK_V2 real firmado. | S |

**Test E8:** además de unit tests, en banco real: drop forzado del 10 % de celdas bulk
(hook de test) → cero NACKs visibles con r=3-4, vídeo sin stall.

---

## 4. Orden de implementación y camino crítico

```
E1 (carrier+cripto+caps) ──┬─▶ E2 (pool bulk, pre-warm)
E8.1 (RS, paralelizable    │
     desde el día 1) ──────┼─▶ E3 (pipeline push + scheduler + decode)  ◀── deps C2/C7 (§0.3)
                           │        └─▶ E7 (créditos)  └─▶ E4′ (adaptación) ─▶ E5
                           └─▶ E1.6 (fallback multi-cell)
```

**Camino crítico:** E1 → E3 → (E7, E4′). **E8.1 (la librería RS) no depende de nada del
túnel** — empezarla en paralelo el primer día; sus unit tests no necesitan red.
**Quick win demo:** E1 + E2 + E3 sin FEC (k=N, r=0) ya reproduce vídeo tunelizado; E8/E7/E4′
lo hacen robusto a velocidad nativa.

---

## 5. Privacidad — qué cambia con E-α

- **Cierra G1:** en `Máxima privacidad`, el broadcaster y el mesh ven la IP del **exit**;
  hop1 ve la IP del viewer pero un flujo bulk opaco de tamaño uniforme; el viewer nunca
  toca con su IP a nadie que sepa qué canal es. Coherente con la tesis (Decisión 1.5, AS-1).
- El etiquetado "Live no tunelizado" de la UI (§12.1 del plan) **se retira** en v8.1.1.
- Tamaños uniformes (E8.3) + push continuo ⇒ menos estructura observable que el camino
  directo (sin patrón request/response por segmento).
- **Límites honestos (documentar en release notes):** hop1 puede inferir "este IP consume
  ~X Mbit/s de vídeo en vivo por eSE" por volumen/timing (igual que el diseño base; la
  correlación de canal exigiría comparar con el patrón del canal — mitigación adicional =
  E-β E11 con claves por subárbol + audiencia). El anonimato real sigue exigiendo ≥3 nodos
  del fork (§8 del plan). El keyword/streamKey llega en claro al exit (Decisión 12.3).
- Cover traffic: desactivado en bulk (el flujo es cobertura); **activo** como hoy en los
  circuitos de control.

---

## 6. Compatibilidad hacia atrás (checklist E)

- [ ] `OP_LIVE_BULK_CELL` = `0xD9` (stub log-and-drop en v8.0.x → silencioso; vanilla
      descarta el opcode de extensión desconocido). Jamás se envía a un peer sin
      `ESE_CAP_TUNNEL_BULK`.
- [ ] `0xD6-0xD8, 0xDA-0xDD` retirados como sustituidos — los defines se quedan
      (append-only), el comentario lo explica; no se reusan para otra cosa.
- [ ] Celda 512 B, cabecera 7 B, sub-protocolo `TUN_OP_*` (append-only, `0x50-0x5F` para E),
      ring de 16 segmentos, bitmap `uint16`: **sin cambios**.
- [ ] `ESE_CAP_TUNNEL_BULK 0x2000` en bit hoy a cero; `0` = peer sin bulk → fallback
      per-symbol multi-cell (E1.6), nunca ruptura.
- [ ] La firma Ed25519 + sha256 del `CHUNK_V2` viaja **íntegra** dentro del FEC — la
      verificación del viewer es byte-a-byte la misma que en directo.
- [ ] `OP_LIVE_CHUNK`/`CHUNK_V2` directos siguen siendo el camino de los modos Directo y
      Tunelizado (control-only) — E no los toca.
- [ ] Gate F2 ampliado: nodo v8.1.1 interopera con vanilla 0.70b, v8.0.0 y v8.1 (sin caps
      bulk → fallback; sin caps dataplane → sin túnel de datos).

---

## 7. Definition of Done — E-α (v8.1.1) · gate F5-E

En el banco real de 3 nodos (FTTH), modo `Máxima privacidad`:

- [ ] Stream de **~2 Mbit/s sostenido 30 min sin stalls** por túnel de 2 saltos (KPI-1).
- [ ] **TTFF < 8 s** (pre-warm E2.4 + gate E5) (KPI-2).
- [ ] **Overhead medido en el path del viewer ≤ 1,20×** (FEC r=3 + padding + cabeceras);
      publicado en release notes (KPI-3).
- [ ] **Kill de un relay mid-stream → sin stall visible** (FEC absorbe + failover E2.3) (KPI-4).
- [ ] CPU de relay < 5 % de un core por stream relayado (KPI-5).
- [ ] El broadcaster **nunca** ve la IP del viewer: verificado con captura (netstat/Wireshark)
      en el broadcaster durante la reproducción — solo conexiones del exit (G1).
- [ ] Cero `OP_LIVE_CHUNK*` directos hacia/desde el viewer en modo `Máxima privacidad` (log).
- [ ] Fallback E1.6 verificado con un hop v8.1 (sin bit bulk) en el circuito.
- [ ] Gate F2: interop vanilla 0.70b + v8.0.0 + v8.1 sin errores.
- [ ] E4′: estrangular un hop → r sube y el vídeo sigue; sin constante 600-800 en el código.
- [ ] Compila x64 Release sin warnings nuevos; endpoints REST nuevos marshaled al main thread.

---

## 8. E-β (= v8.1.2) — definición a nivel de plan

> Se desglosará en su propio breakdown al terminar E-α (práctica del proyecto: el desglose
> ejecutable se escribe contra el código real del momento). Alcance comprometido:

| ID | Tarea | Tam | Dep | Qué entrega |
|---|---|---|---|---|
| E9 | **Split de cripto:** payload bulk cifrado 1 vez e2e (clave de sesión viewer↔exit del subscribe C1) + MAC ligero por hop. Prerequisito de E11; baja la CPU de relays. | M | E1 | menos CPU; habilita dedup |
| E10 | **Bulk sobre uTP** (libutp vendorizado, `eMuleAI/UtpSocket.cpp`). **Gate:** solo si las mediciones F5-E muestran jitter TCP-over-TCP relevante. | M | E1 | menos jitter bajo pérdida |
| E11 | **Dedup multicast por subárbol:** clave `K_sub` por (sesión-exit, hop1) entregada por el túnel de control; el exit cifra cada símbolo 1 vez por subárbol; hop1 reenvía el mismo ciphertext a sus circuitos hijos con re-wrap de cabecera. Coste agregado →~1,2-1,5×/viewer con audiencia. Las celdas direccionadas por contenido (`seq+symbol_idx`, §2.2) lo permiten **sin tocar el wire de E-α**. | L | E9, C7 | escala: 3× → ~1,3× |
| E12 | **Viewer-as-relay:** nodos HighID relayan canales **asignados aleatoriamente** (nunca el que ven; decorrelación), capado a la subida ociosa medida; opt-out en preferencias. | M-L | D4 | la oferta de relays escala con la audiencia; crece el anonymity set |

KPI E-β: coste agregado medido ≤1,6× por viewer con ≥2 viewers tras el mismo exit;
≥50 % de los nodos viewer aportando relay en el banco de pruebas.

---

## 9. Gotchas

- **Main thread:** todo el envío de celdas (bulk incluidas) toca sockets → main thread only,
  como el resto de `CLiveTunnel`. El encode/decode RS (~2-5 ms por segmento de 500 KB) es
  asumible en el main thread; si F5-E muestra hitches, mover encode a worker con handoff
  marshaled (patrón v0.72), **no** al revés.
- **`OnionEncrypt` asume ≤512 B** (buffers de S-OPT-1): parametrizar tamaño en E1.4, no
  duplicar la función para bulk.
- **No re-fragmentar símbolos:** si un símbolo no cabe en la ventana de créditos, espera;
  trocearlo rompería "1 símbolo = 1 celda = unidad de NACK".
- **El exit retiene segmentos codificados** solo `ESE_LIVE_MAX_SEGMENTS` (16 × 2 s = 32 s),
  alineado con el ring — un NACK más viejo se responde con "gone" implícito (sin re-push).
- **Drain antes de rotar** (E2.3): rotar con símbolos en vuelo = NACKs espurios + paridad
  desperdiciada. La rotación de bulk espera frontera de segmento **siempre**.
- **Créditos por circuito, no por sub:** un circuito lento no debe frenar los stripes de
  los demás (eso era el HoL que E3 elimina).
- **`weak_ptr` de circuito en `BulkSub`** (patrón A6): una sub no mantiene vivo un circuito
  rotado; el sweep la migra al `BulkCircuitSet` vigente.
- **Cuidado con `sURL.Left(N)`** al añadir endpoints REST de telemetría (colisiones de
  prefijo, regla §7 del plan).
- **El registro CHUNK_V2 se pad-ea ANTES del FEC** — el padding viaja dentro de los
  símbolos. No padear las celdas (rompería "símbolos uniformes").
- **r=0 es válido** (k=N): úsalo para el hito de demo E3 antes de integrar E8 — el wire
  no cambia al activar la paridad.

---

## 10. Estimación honesta

| Bloque | Tam | Nota |
|---|---|---|
| E1 carrier+cripto+caps+fallback | L | el fallback E1.6 es la mitad del trabajo |
| E2 pool bulk + pre-warm | M | absorbe E6 |
| E3 pipeline push + scheduler + decode | L | **el corazón**; E3.1 depende del estado real de C7 |
| E4′ adaptación medida | M | sustituye al cap fijo |
| E5 gate condicional | S | |
| E7 créditos | M | |
| E8 FEC+padding | L | E8.1 paralelizable desde el día 1; absorbe E13 |
| **E-α total (v8.1.1)** | 3L+3M+1S | **~3 semanas** (1 dev) |
| E-β total (v8.1.2) | 1L+2M+1M-L | **~2 semanas** (1 dev) |

> El plan original estimaba Sprint E en ~2 semanas, pero **no incluía** el pipeline push del
> exit (E3.1-E3.2, asumía que "los chunks van por el túnel" sin decir cómo llegan al túnel),
> ni el FEC, ni el fallback de capacidad. Las ~3 semanas de E-α son el alcance real; a
> cambio, el resultado es bitrate nativo y no 600-800 kbps.

---

*Desglose derivado de exploración directa del código 2026-06-11 (`LiveTunnel.h`,
`LiveCellQueue.h`, `LiveProtocol.h`, `opcodes.h`, `LiveStreamManager.*`). Sub-spec de
`V8.1_SPRINT_PLAN.md` §Sprint E re-especificado (decisión §12.4) según el análisis de
`V8.1.1_SPRINT_E_SPEED.md`. Requiere Sprint A (commit `2c9bfeb`) y las interfaces C2/C5/C7.*
