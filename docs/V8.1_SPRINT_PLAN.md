# v0.70b-eSE 8.1 — Plan de Sprint: Data Plane por Tunnel Onion

> **Objetivo de la versión:** que las operaciones reales (búsqueda Kad y suscripción LiveTV en **v8.1**; transferencia de chunks en **v8.1.1**) circulen por el tunnel onion de 2 saltos cuando el modo de privacidad lo pida. Hoy solo lo hacen los endpoints demo `tunnel_ping` y `tunnel_search`.
>
> **Prioridades, en orden:** (1) que funcione bien y de forma correcta; (2) buena velocidad. Cada optimización de velocidad detectada está documentada en la §6.
>
> Este plan procede de una auditoría de código en 4 pasadas (transporte, búsqueda Kad, streaming LiveTV, selector de modo). Las referencias `archivo:línea` son del estado del repo a 2026-05-20.

---

## 1. Resumen ejecutivo

v8.0.0 entregó un transporte onion **funcional pero sin uso real**: cripto X25519 + HKDF-SHA256 + ChaCha20-Poly1305, circuitos de 2 saltos, celdas de 512 B. El problema es que ese transporte solo sabe hacer **un request → una respuesta, ambos ≤473 B, de forma síncrona y bloqueante**. No hay fragmentación, no hay dispatcher genérico, el modo de privacidad es código muerto y el PST pool nunca se llena.

**Decisión estratégica (la más importante de v8.1):** separar *plano de control* de *plano de datos*.

- **Plano de control** (búsqueda Kad, subscribe, announce, heartbeat, peer-list): paquetes pequeños, y son justo los que revelan **quién ve qué**. Tunelizarlos es el 90 % del valor de privacidad al ~1 % del coste de ancho de banda. **Esto es el núcleo de v8.1.**
- **Plano de datos** (chunks de vídeo): 2 Mbit/s por un onion de 2 saltos = ~6 Mbit/s agregados en los relays, ~1083 celdas por segmento de 500 KB. Tunelizar esto **no "funciona a buena velocidad"** sin infraestructura nueva (celdas bulk, FEC+multipath, circuitos dedicados, entrega adaptativa medida). Se entrega en **v8.1.1** (Sprint E-α, bitrate nativo) y **v8.1.2** (Sprint E-β, escala) — ver §12.1 y §12.4. **Sin cap fijo de bitrate**: el objetivo es velocidad nativa con ~10-15 % de overhead, no 600-800 kbps (análisis en [`V8.1.1_SPRINT_E_SPEED.md`](V8.1.1_SPRINT_E_SPEED.md)).

Resultado: el modo **`Tunelizado`** por defecto tuneliza el control y deja los chunks directos; el modo **`Máxima privacidad`** tuneliza todo. La §8 documenta honestamente qué protege cada uno.

---

## 2. Estado actual (auditoría)

### 2.1 Lo que YA funciona
- Construcción de circuitos 1 y 2 saltos: `CELL_CREATE/CREATED`, `CELL_EXTEND/EXTENDED` (`LiveTunnel.cpp:75-159, 295-376, 476-746`).
- Self-loopback para setups de test con 2 PCs (`HandleExtend_Relay`, `LiveTunnel.cpp:581-656`).
- Cripto onion por capas, nonce por dirección (`LiveCircuit.cpp:49-126`).
- Cover traffic Poisson independiente — **no añade latencia al tráfico real** (`LiveCoverTraffic.cpp:16-33`).
- Demos `tunnel_ping` / `tunnel_search` end-to-end (`WebServer.cpp:5137-5210`).
- `TAG_ESE_CAPS` (tag `0x6C`) negociado en `OP_HELLO`, bit `PRIVACY_TUNNELING 0x100` (`Opcodes.h:499-516`, `BaseClient.cpp:629-637`).

### 2.2 Lo que está STUB o es CÓDIGO MUERTO
| Elemento | Estado | Ref |
|---|---|---|
| `TUN_OP_LIVE_SUBSCRIBE` / `SUB_ACK` | Declarado en el enum, **nunca referenciado** | `LiveTunnel.h:105-106` |
| Opcodes `OP_LIVE_T_*` (`0xD6-0xDD`) | Reservados, handler loguea `[STUB F0]` | `ListenSocket.cpp:2142-2153` |
| `ShouldRouteThroughTunnel()` | Existe, **sin llamadas** fuera de su propia doc | `LiveTunnel.cpp:999-1008` |
| `CKadV2TunnelPool` (PST pool) | `RegisterTunnel()` nunca se llama → `Size()` siempre 0; pre-build del successor es un comentario | `KadV2TunnelPool.cpp:49-73` |
| `m_fallback` (STRICT/BALANCED/BEST_EFFORT) | Se guarda, **nunca se lee** ninguna lógica | `KadV2ModeSelector.h:38-44` |
| `CCellQueue m_sendQ` | `Pop` nunca se invoca; sin flush loop | `LiveTunnel.cpp:230` |
| `tunnel_search` | **No hace búsqueda Kad** — substring match contra el cache local del exit | `LiveTunnel.cpp:942-972` |
| Persistencia del modo de privacidad | **No hay claves INI** — se pierde en cada reinicio | `Preferences.cpp` (grep vacío) |
| Kad Search v2 (M1-M6) | Scaffolding; **v8.1 NO depende de ello** | `KadV2*.cpp` |

### 2.3 Carencias del transporte (bloqueantes para v8.1)
1. **Sin framing multi-cell.** 1 request = 1 celda de 512 B = ≤473 B útiles a 2 saltos. Un set de resultados Kad o un chunk no caben.
2. **Sin dispatcher genérico.** Cada `TUN_OP` está hardcodeado en `HandleRelay_Exit`.
3. **Solo API bloqueante.** `TunnelPing` hace `Sleep(50)`-poll en el hilo del WebServer, timeout fijo 3 s — incapaz de ver una búsqueda Kad real de hasta 45 s.
4. **El exit no hace I/O saliente real.** Solo lee su cache local; no puede ejecutar una `CSearch` ni reenviar un subscribe.
5. **Fuga de la pending-map.** Sin sweeper: una request abandonada no se limpia nunca.

---

## 3. Arquitectura objetivo

```
                 ┌─────────────────── plano de control (v8.1 núcleo) ───────────────────┐
  Usuario ──▶ CKadV2ModeSelector.Decide() ──▶ ¿Tunelizado?
                 │                                   │
                 │ no                                │ sí
                 ▼                                   ▼
            path directo                  CLiveTunnel (transporte genérico v2)
            (eD2K/Kad clásico)             ├─ fragmentación/reassembly multi-cell
                                           ├─ dispatcher sub_cmd → handler
                                           ├─ API async (callback/evento, sin poll)
                                           ├─ pinning de mensaje a 1 circuito + retry
                                           └─ PST pool pre-calentado
                                                     │
                              ┌──────────────────────┼───────────────────────┐
                              ▼                      ▼                       ▼
                       TUN_OP_KAD_SEARCH    TUN_OP_LIVE_SUBSCRIBE     TUN_OP_LIVE_CTRL
                       (exit corre CSearch  (exit reenvía al           (announce/heartbeat/
                        real, streaming)     broadcaster real)          peer-list)

                 └─────────────────── plano de datos (Sprint E → v8.1.1/v8.1.2) ───────────────────┘
                  OP_LIVE_BULK_CELL 0xD9 (símbolos FEC de 16 KB) ──▶ pool bulk dedicado
                  push exit→viewer · striping multipath · créditos · ABR por goodput medido
```

Piezas nuevas a construir, por capa:
- **Transporte:** framing multi-cell, dispatcher, API async, sweeper (Sprint A).
- **Aplicación:** exit que ejecuta `CSearch` real + bridge async (Sprint B); subscribe/control tunelizado (Sprint C).
- **Control:** plumbing del modo, persistencia, política de fallback, PST pool, UI (Sprint D).
- **Bulk:** celda bulk 0xD9, FEC+striping multipath, circuitos dedicados, pipeline push, entrega adaptativa medida (Sprint E-α); dedup multicast + viewer-relays (Sprint E-β).

---

## 4. Sprints

Tamaños: **S** ≈ ½ día · **M** ≈ 1-2 días · **L** ≈ 3-5 días · **XL** ≈ 1-2 semanas.
`Dep` = micro-sprints que deben estar listos antes.

### Sprint A — Transporte genérico tunelizado (FUNDACIÓN)
> Sin esto, nada del resto es posible. Es la prioridad nº 1.
> Desglose ejecutable por sub-tareas (A1.1, A1.2…): [`V8.1_SPRINT_A_BREAKDOWN.md`](V8.1_SPRINT_A_BREAKDOWN.md).

| ID | Tarea | Archivos | Tamaño | Dep |
|---|---|---|---|---|
| A1 | **Framing multi-cell.** Sub-cabecera de fragmentación `stream_id(4)+seq(2)+total_len(4)+flags(1)` **dentro del payload** (no tocar la celda de 512 B ni la cabecera estable de 7 B). Buffers de reassembly en originador y exit. Nuevos `TUN_OP_*` para ops multi-cell. | `LiveTunnel.cpp/.h`, `LiveCellQueue.*` | L | — |
| A2 | **Dispatcher genérico** `sub_cmd → handler` en el exit (tabla de registro; sustituye el `switch` hardcodeado de `HandleRelay_Exit`). | `LiveTunnel.cpp` | M | — |
| A3 | **API async request/response.** Callbacks/futures; evento o `CONDITION_VARIABLE` señalizado por el hilo de socket en lugar de `Sleep(50)`-poll. | `LiveTunnel.cpp/.h`, `WebServer.cpp` | M | A2 |
| A4 | **Pinning + retry.** Todas las celdas de un mensaje fragmentado van por **un solo circuito** (orden de nonce + reassembly estable); si el circuito muere mid-mensaje, retry en otro. | `LiveTunnel.cpp` | M | A1 |
| A5 | **Sweeper de TTL.** Barre con TTL configurable (5-10 s) **tanto** la *pending-map* del originador **como** los *buffers de reassembly* (originador y exit): un circuito que muere mid-mensaje deja celdas parciales huérfanas → fuga de memoria si no se barren. | `LiveTunnel.cpp` | S | A1,A3 |
| A6 | **Framework de I/O saliente en el exit.** Permite al exit lanzar una operación de red real y hacer streaming de la respuesta por celdas. | `LiveTunnel.cpp` | M | A1,A2 |
| A7 | Negociación de capacidad: nuevo bit `TAG_ESE_CAPS` `ESE_CAP_TUNNEL_DATAPLANE 0x1000` ("0" = peer v8.0.0, solo single-cell). | `Opcodes.h`, `BaseClient.cpp` | S | — |
| A8 | **`TunnelEndpoint` opaco** — **YA EXISTE** (`LiveTunnel.h:36-39`): `{circ_id, remote_pubkey[32]}`, identidad de peer sin IP ni puntero (tesis §5.6.3). Sprint A no añade nada; el consumo real es C6. | `LiveTunnel.h` | — | — |

### Sprint B — Búsqueda Kad por tunnel
> Convierte `tunnel_search` (demo de cache local) en una búsqueda Kad nativa real.

| ID | Tarea | Archivos | Tamaño | Dep |
|---|---|---|---|---|
| B1 | El exit ejecuta una **`CSearch` real** (`PrepareFindKeywords`) en vez de leer su cache. | `LiveTunnel.cpp`, `kademlia/.../SearchManager.cpp` | L | A6 |
| B2 | **Bridge del callback async de Kad.** `ProcessResultKeyword` (hasta 45 s) → emisión incremental de celdas `TUN_OP_KAD_RESULT` según llegan resultados. | `kademlia/.../Search.cpp`, `LiveTunnel.cpp` | L | A1,A3,B1 |
| B3 | **Registro de resultado rico** (TLV): filehash, tamaño, `TAG_SOURCEIP/PORT/UPORT`, tags `TAG_ESE_LIVE_*`, AICH. Sustituye el string `title\|bitrate\|viewers`. | `LiveTunnel.cpp`, `LiveKadBridge.cpp` | M | A1 |
| B4 | Búsqueda **dual-namespace** (hash limpio `EseLiveGetKeywordHash` + hash legacy `KadGetKeywordHash`). | `LiveKadBridge.cpp` | S | B1 |
| B5 | **Entrega incremental al cliente** (resultados visibles según llegan; sin espera ciega de 45 s). | `WebServer.cpp`, UI | M | B2 |
| B6 | **Cache de resultados en el exit** por keyword-hash con TTL corto (ver §6 S-OPT-8). | `LiveTunnel.cpp` | M | B1 |
| B7 | **`TUN_OP_KAD_CANCEL`** — el viewer notifica al exit que aborte la búsqueda y destruya el objeto `CSearch`. El exit **también** cancela si el circuito muere — no dejar una búsqueda de 45 s huérfana gastando CPU/ancho de banda. | `LiveTunnel.cpp`, `kademlia/.../SearchManager.cpp` | M | B1,A2 |

### Sprint C — Subscribe y plano de control LiveTV por tunnel

| ID | Tarea | Archivos | Tamaño | Dep |
|---|---|---|---|---|
| C1 | Implementar **`TUN_OP_LIVE_SUBSCRIBE` / `SUB_ACK`** (sender + handler exit). El subscribe **establece una sesión tunelizada**: el viewer queda representado por un `TunnelEndpoint`; **nunca** se devuelve una IP cruda del broadcaster al viewer (ver §13.2). | `LiveTunnel.cpp/.h` | M | A2,A3,A8 |
| C2 | El **exit reenvía `OP_LIVE_SUBSCRIBE`** al broadcaster en nombre del viewer; la respuesta (`OP_LIVE_PEER_LIST`, `HEARTBEAT`) vuelve por el tunnel. | `LiveTunnel.cpp`, `LiveStreamManager.cpp` | L | A6,C1 |
| C3 | Tunelizar **announce / heartbeat / peer-list** (paquetes de control que revelan quién-ve-qué). | `LiveStreamManager.cpp`, `LiveTunnel.cpp` | M | C2 |
| C4 | Decidir y cerrar los handlers `OP_LIVE_T_*` (`0xD6-0xDD`): o se implementan, o se documentan como sustituidos por el sub-protocolo `TUN_OP_*` y se retira el stub. | `ListenSocket.cpp`, `Opcodes.h` | S | C1 |
| C5 | **Handoff control→datos:** tras subscribe tunelizado, en modo `Tunelizado` los chunks van directos; en `Máxima privacidad`, por el bulk tunnel (Sprint E). | `LiveStreamManager.cpp` | M | C2 |
| C6 | **Re-indexar `CLiveMeshManager`** de `CUpDownClient*` a `TunnelEndpoint` (tesis §5.6.3). El rarest-first scheduling no cambia, opera sobre endpoints. **Arregla a la vez el micro-corte**: el swap del objeto `CUpDownClient` deja de expulsar al peer del mesh (ver §13.1). | `LiveMeshManager.h/.cpp` | L | A8,C2 |
| C7 | **Exit como proxy multicast por canal.** Si N viewers se suscriben al mismo `streamKey` por el mismo exit, el exit registra **una sola** suscripción ante el broadcaster y asocia los N `TunnelEndpoint` localmente — evita agotar sockets y disparar el rate-limit por-IP del broadcaster. (El fan-out de *datos* se completa en E3.) | `LiveTunnel.cpp`, `LiveStreamManager.cpp` | L | C2,C6 |

### Sprint D — Plumbing del modo de privacidad

| ID | Tarea | Archivos | Tamaño | Dep |
|---|---|---|---|---|
| D1 | Llamar `ShouldRouteThroughTunnel()` / `Decide()` en los **sitios de dispatch reales** (búsqueda Kad, subscribe). Hoy es código muerto. | `SearchManager.cpp`, `LiveStreamManager.cpp` | M | B1,C2 |
| D2 | **Persistencia INI** de `mode` / `fallback` / `sensitive-keywords` (claves nuevas, leídas con default = Adaptive/Balanced). | `Preferences.cpp/.h` | S | — |
| D3 | Implementar la **política `m_fallback`**: STRICT (aborta), BALANCED (reduce fanout), BEST_EFFORT (cae a Directo + avisa). | `KadV2ModeSelector.cpp` | M | D1 |
| D4 | **Poblar el PST pool:** `RegisterTunnel()` desde `BuildPool`; implementar el pre-build del successor (hoy comentario). | `KadV2TunnelPool.cpp`, `LiveTunnel.cpp` | M | A1 |
| D5 | Wire `Acquire()` en el search path; match de destino **XOR-closest** (hoy devuelve cualquier circuito Active). | `KadV2TunnelPool.cpp` | M | D4 |
| D6 | **UI MFC:** dropdown real de modo (Preferencias→Conexión o `NetworkInfoDlg`); hoy solo hay texto read-only. | `NetworkInfoDlg.cpp`, `emule.rc` | M | D2 |
| D7 | **Per-operation mode:** campo `operationClass` en `QueryContext` para decidir tunnel por operación, no global. | `KadV2ModeSelector.*` | S | D1 |
| D8 | **Telemetría de túnel.** Métricas en `NetworkInfoDlg` + `/api/live/privacy`: circuitos activos en el PST pool, RTT medio del túnel, bytes/celdas tunelizados vs. directos. El usuario verifica que el túnel va fino. | `NetworkInfoDlg.cpp`, `WebServer.cpp` | M | D4 |

### Sprint E — Plano de datos / chunks por tunnel → **v8.1.1 (E-α) + v8.1.2 (E-β)**
> **DECIDIDO (§12.1 + §12.4):** E-α se entrega como **v8.1.1**, la release inmediatamente posterior a v8.1; cierra G1 (anonimato de IP del viewer) **a bitrate nativo**, sin el cap de 600-800 kbps de la especificación original. E-β (**v8.1.2**) escala el coste agregado de ~3× a ~1,3×. No es "opcional" — la tesis (Decisión 1.5) exige túnel always-on para Live.
> Análisis de alternativas: [`V8.1.1_SPRINT_E_SPEED.md`](V8.1.1_SPRINT_E_SPEED.md). Desglose ejecutable: [`V8.1.1_SPRINT_E_BREAKDOWN.md`](V8.1.1_SPRINT_E_BREAKDOWN.md) — incluye el wire format definitivo (la celda bulk es `0xD9`, repurposing del stub `OP_LIVE_T_CHUNK`: el rango `0xD0-0xDF` resultó estar completo).

**E-α (v8.1.1) — bitrate nativo por el túnel:**

| ID | Tarea | Archivos | Tamaño | Dep |
|---|---|---|---|---|
| E1 | **Carrier bulk `OP_LIVE_BULK_CELL` (`0xD9`)**: celda variable ≤~17 KB, forwarding por hop, AEAD onion por capa, cap bit `ESE_CAP_TUNNEL_BULK 0x2000`, fallback per-symbol multi-cell para hops sin la cap. Reduce un segmento de 500 KB de ~1083 celdas a ~32. Cierra C4 (resto de stubs `OP_LIVE_T_*` retirados como sustituidos). | `Opcodes.h`, `ListenSocket.cpp`, `LiveTunnel.cpp`, `LiveCircuit.cpp` | L | A1,A7 |
| E2 | **Pool de circuitos bulk dedicado** por stream (2-4, hop1 disjuntos): sin cover traffic, rotación solo en frontera de segmento con drain, **pre-warm en `JoinStream`** (absorbe E6). | `LiveTunnel.cpp`, `LiveStreamManager.cpp`, `KadV2TunnelPool.cpp` | M | D4,E1 |
| E3 | **Pipeline push exit→viewer**: ingest headless de chunks en el exit para subs proxied (si C7 no lo entregó), registro de subs bulk por `TunnelEndpoint`, **scheduler de símbolos** (pin símbolo→circuito, stripes entre circuitos — sin HoL), gap-fill por NACK, decode+inyección en el handler `CHUNK_V2` existente del viewer. | `LiveTunnel.cpp`, `LiveStreamManager.cpp` | L | E1,E8,C2,C7 |
| E4′ | **Entrega adaptativa medida** (sustituye al "bitrate adaptativo 600-800"): EWMA de goodput/pérdida por circuito, **adaptación de la paridad FEC r** (2..5) y rebalanceo de stripes; ante shortfall real aplica la política D3 con aviso honesto. **El bitrate de la fuente no se toca; sin constante 600-800.** | `LiveTunnel.cpp`, `LiveStreamManager.cpp`, `KadV2ModeSelector.cpp` | M | C5,E3 |
| E5 | Gate de playback **condicional**: se queda en 2 segmentos con FEC activo; sube a 3 solo en fallback multi-cell o churn alto (r=5). | `LiveStreamManager.cpp` | S | C5,E8 |
| E7 | **Backpressure por créditos** (`BULK_ACK`): ventana > producto banda-retardo (32 símbolos ≈ 512 KB), por circuito; sensibilidad a la cola del socket local; sweep TTL de subs muertas. **No** stop-and-wait. | `LiveTunnel.cpp` | M | E1 |
| E8 | **FEC Reed-Solomon sistemático GF(256)** (k≤64, r≤8, símbolo = celda bulk de 16 KB, autocontenido sin dependencia nueva) + serialización del `CHUNK_V2` íntegro (firma Ed25519 incluida) + **padding cuantizado a múltiplos de símbolo** (absorbe E13; ~1-2 % overhead, celdas de tamaño uniforme). La pérdida de celdas o la muerte de un circuito deja de ser stall: cualquier k de k+r reconstruye. | nuevos `LiveFec.cpp/.h`, `LiveTunnel.cpp` | L | E1 |

**E-β (v8.1.2) — escala (coste agregado →~1,3×):** E9 split de cripto (payload e2e 1 vez + MAC por hop), E10 bulk sobre uTP (condicional a medición), E11 **dedup multicast por subárbol** (claves `K_sub` por (sesión-exit, hop1); generaliza C7 un nivel), E12 **viewer-as-relay** (HighID relayan canales asignados aleatoriamente). Definición a nivel de plan en el breakdown §8; desglose ejecutable propio al terminar E-α.

### Sprint F — Testing y verificación

| ID | Tarea | Tamaño |
|---|---|---|
| F1 | Tests de loopback (self-loopback 2-PC ya soportado): circuito, fragmentación, dispatcher. | M |
| F2 | **Gates de regresión:** interop con eMule vanilla 0.70b (debe ignorar todo lo nuevo) e interop con fork v7.x / v8.0.0 (debe caer a single-cell). | M |
| F3 | Tests de fragmentación/reassembly: mensajes grandes, circuito que muere mid-mensaje, requests abandonadas (sweeper). | M |
| F4 | Verificación empírica end-to-end estilo v8.0.0: 2 PCs reales, REST API, búsqueda Kad real anónima confirmada. | M |
| F5 | **Medición de velocidad:** latencia search tunelizada vs directa; throughput; tiempo de build de circuito con/sin PST pool. Tabla de resultados en las release notes. | M |

---

## 5. Orden de ejecución y dependencias

```
A1 ─┬─▶ A4 ─┐
A2 ─┼─▶ A3 ─┼─▶ A5
A6 ─┘       │
A7          │
            ├─▶ B1 ─▶ B2 ─▶ B5
            │         B6, B7
            │   B3 ─▶ B4
            ├─▶ C1 ─▶ C2 ─▶ C3
            │              C4, C5, C6, C7
            └─▶ D1 ─▶ D3, D7
        D2 ─▶ D6
        A1 ─▶ D4 ─▶ D5
  (núcleo v8.1 = A+B+C+D+F)  ──▶  E-α: E1,E2,E3,E4′,E5,E7,E8 (= v8.1.1)  ──▶  E-β: E9..E12 (= v8.1.2)
```

**Camino crítico:** A1 → A4/A6 → B1 → B2 → B5, y A2/A3 → C1 → C2 → C3. Sprint D se solapa con B/C. Sprint E es posterior (consume las interfaces C2/C5/C7); dentro de E-α el camino crítico es E1 → E3 → E7/E4′, con E8.1 (librería FEC) paralelizable desde el día 1.

---

## 6. Optimizaciones de velocidad (sección dedicada)

Catálogo exhaustivo de mejoras de velocidad detectadas en la auditoría. **Impacto** es estimado; medir en F5.

### 6.1 Hot path del transporte
| ID | Optimización | Dónde | Impacto | Riesgo |
|---|---|---|---|---|
| S-OPT-1 | Buffers de scratch en pila en vez de `std::vector` en `OnionEncrypt`/`OnionDecryptAll` (hoy 3-5 mallocs por celda; las celdas están acotadas a ≤512 B). | `LiveCircuit.cpp:58-126` | Alto en CPU bajo carga | Bajo |
| S-OPT-2 | Reusar el objeto cipher ChaCha20-Poly1305 por hop en vez de `SetKeyWithIV` (re-keying completo) en cada celda. | `LiveOnionCrypto.cpp` | Medio | Bajo |
| S-OPT-3 | Sustituir el `Sleep(50)`-poll de `TunnelPing`/`TunneledKadSearch` por evento/CV señalizado por el hilo de socket. Elimina 0-50 ms de latencia artificial. | `LiveTunnel.cpp:1010-1121` | Alto en latencia | Bajo |
| S-OPT-4 | Coalescer varias celdas listas en **una sola escritura TCP** (hoy 1 celda = 1 `Packet` = 1 write). | `LiveTunnel.cpp:204-233` | Medio | Medio |
| S-OPT-5 | Lock **por circuito** en vez de un único `m_lock` global que serializa todos los circuitos. | `LiveTunnel.cpp` | Alto con varios circuitos | Medio |
| S-OPT-6 | Eliminar el `m_sendQ` muerto, o cablearlo con un flush loop más rápido que el tick de 1 Hz. | `LiveTunnel.cpp:230` | Bajo (limpieza) | Bajo |
| S-OPT-7 | `bForceImmediateSend` para celdas de control sensibles a latencia (hoy entran en la cola de control-packets de eMule). | `LiveTunnel.cpp:70` | Medio en latencia | Bajo |

### 6.2 Búsqueda Kad
| ID | Optimización | Dónde | Impacto | Riesgo |
|---|---|---|---|---|
| S-OPT-8 | **Cache de resultados en el exit** por keyword-hash con TTL corto — repeticiones se sirven al instante. | Sprint B6 | Alto en queries repetidas | Bajo |
| S-OPT-9 | Prefetch de keywords populares en el exit (cache caliente). | `LiveTunnel.cpp` | Medio | Medio |
| S-OPT-10 | **Latency hedging:** lanzar la misma búsqueda por 2-3 circuitos y quedarse con la primera respuesta. `SendThrough` ya hace round-robin. | `LiveTunnel.cpp:204-233` | Alto en latencia (peor caso) | Medio (más tráfico) |
| S-OPT-11 | Subir `ALPHA_QUERY` (hoy 3) para la `CSearch` del exit — el exit no paga coste de anonimato en el lado Kad. | `kademlia/.../Defines.h:45` | Medio | Medio |
| S-OPT-12 | Reusar el timeout adaptativo `fastKad` (media + 2σ) en la `CSearch` del exit. | `kademlia/.../Search.cpp:307` | Medio | Bajo |
| S-OPT-13 | Entrega **incremental/streamed** de resultados (el usuario ve hits según llegan, no tras 45 s). | Sprint B2/B5 | Alto en latencia percibida | Bajo |

### 6.3 LiveTV / plano de datos
| ID | Optimización | Dónde | Impacto | Riesgo |
|---|---|---|---|---|
| S-OPT-14 | **Celdas bulk de 16 KB** (vs 512 B): un segmento de 500 KB pasa de ~1083 a ~32 celdas → ~30× menos llamadas de cripto y overhead de cabecera. 1 celda = 1 símbolo FEC. | Sprint E1/E8 | Muy alto en throughput tunelizado | Medio |
| S-OPT-15 | Pin de **símbolo** a un circuito; stripes de un mismo segmento repartidos entre circuitos (multipath real, sin HoL intra-segmento ni inter-circuito). | Sprint E3 | Alto | Bajo |
| S-OPT-16 | Pre-warm de circuitos durante `JoinStream` (Active antes del primer push). | Sprint E2 | Alto en time-to-first-frame | Bajo |
| S-OPT-17 | Circuitos bulk dedicados: sin cover traffic, rotación solo en frontera de segmento (cadencia ~30 s de la Decisión 5.3; el 5 min actual es valor temporal de test). | Sprint E2 | Medio-Alto | Medio (rotación = privacidad) |
| S-OPT-18 | ~~Bitrate adaptativo a 600-800 kbps~~ **SUSTITUIDA por E4′** (decisión §12.4): la fuente no se degrada; se adapta la **paridad FEC y el reparto de stripes** al goodput medido. Ver `V8.1.1_SPRINT_E_SPEED.md` §4.1. | Sprint E4′ | Muy alto en viabilidad | Bajo |
| S-OPT-19 | Desactivar cover traffic en el path bulk — el propio flujo de chunks ya es cobertura. | Sprint E2 | Medio | Bajo |
| S-OPT-20 | **Default control-plane-only** (chunks directos): es la decisión de velocidad más grande del plan — privacidad del grafo social al ~1 % del coste de ancho de banda. | §1, Sprint C5 | Decisivo | — (ver §8) |
| S-OPT-27 | **Exit como proxy multicast** (C7): el broadcaster envía el canal **una sola vez** al exit aunque haya N viewers detrás → menos CPU/sockets en el emisor y no dispara su rate-limit por-IP. | Sprint C7, E3 | Alto en escalabilidad | Medio |
| S-OPT-28 | **FEC + striping multipath** (RS sistemático, r adaptativo 2-5): la pérdida de celdas o la muerte de un circuito deja de costar stalls/retransmisiones (1 RTT de túnel por pérdida ahorrado). Es lo que permite bitrate nativo sin cap. | Sprint E8/E4′ | Decisivo en viabilidad | Bajo |
| S-OPT-29 | **Dedup multicast por subárbol** (claves `K_sub`): coste agregado por viewer 3× → ~1,2-1,5× con audiencia; el mismo símbolo no viaja N veces por el mismo enlace. | Sprint E11 (v8.1.2) | Muy alto en escala | Medio |
| S-OPT-30 | **Viewer-as-relay** (asignación aleatoria de canal): la capacidad de relay escala con la audiencia, como en el mesh directo; además crece el anonymity set. | Sprint E12 (v8.1.2) | Alto en escala | Medio |
| S-OPT-31 | **Padding cuantizado a múltiplos de símbolo** (~1-2 %): celdas bulk de tamaño uniforme — debilita el fingerprint por tamaños a la vez que uniformiza el FEC. | Sprint E8 | Medio (privacidad+robustez) | Bajo |

### 6.4 Modo y PST pool
| ID | Optimización | Dónde | Impacto | Riesgo |
|---|---|---|---|---|
| S-OPT-21 | **Pre-calentar el PST pool de verdad** — hoy `Tick()` marca un successor pero no construye nada. Ahorra el ~600 ms de build por query. | Sprint D4 | Alto en latencia primera query | Bajo |
| S-OPT-22 | **Build de circuito en paralelo:** lanzar el `EXTEND` del hop2 en cuanto llega el `CREATED` del hop1; successors concurrentes, no en serie. | `LiveTunnel.cpp:75-159` | Alto en build time | Medio |
| S-OPT-23 | **Fast-failover:** ping de liveness; al morir un hop, saltar a un circuito caliente del pool en vez de reconstruir. | `LiveTunnel.cpp`, Sprint D4 | Alto en peor caso | Medio |
| S-OPT-24 | **Adaptive consciente de latencia:** EWMA de RTT por circuito (muestreado en cada reply); `Decide()` tuneliza solo mientras el RTT esté bajo umbral. | `KadV2ModeSelector.cpp` | Medio-Alto | Medio |
| S-OPT-25 | `Acquire()` con match de destino XOR-closest (menos saltos efectivos). | Sprint D5 | Medio | Bajo |
| S-OPT-26 | Fallback por tasa de éxito: monitorizar ratio de éxito; bajo BEST_EFFORT cae a Directo automáticamente. | Sprint D3 | Medio (UX) | Bajo |

> **Quick wins** (alto impacto / bajo riesgo, hacer primero): S-OPT-1, S-OPT-3, S-OPT-13, S-OPT-16, S-OPT-21.

---

## 7. Compatibilidad hacia atrás (checklist obligatorio)

Regla del proyecto: **todo cambio de wire/fichero/formato debe funcionar con eMule 0.70b vanilla Y con versiones previas del fork (v7.x, v8.0.0).** Sin excepciones.

- [ ] `OP_LIVE_TUNNEL_CELL` (`0xD5`) viaja sobre el protocolo de extensión: vanilla descarta el opcode desconocido en silencio. ✓ ya seguro.
- [ ] **No cambiar** el tamaño de celda (512 B) ni la cabecera estable de 7 B del sub-protocolo. La fragmentación va **dentro** del payload (A1).
- [ ] `TUN_OP_*` es append-only (enum de 1 byte). Valores nuevos libres; nunca reordenar.
- [ ] Nuevo bit `TAG_ESE_CAPS` en un bit hoy a cero (`0x1000`); `0` = peer legacy sin data-plane v8.1. No repropiar bits `0x1-0x800`.
- [ ] Negociación A7: si **algún hop** del circuito es v8.0.0, caer a framing single-cell. El originador conoce las caps de cada hop.
- [ ] `OP_LIVE_BULK_CELL` (E1) = **`0xD9`** (repurposing del stub `OP_LIVE_T_CHUNK`; el rango `0xD0-0xDF` está completo — auditoría 2026-06-11). Stub log-and-drop en v8.0.x → silencioso; jamás se envía a un peer sin `ESE_CAP_TUNNEL_BULK 0x2000`. Resto de stubs `OP_LIVE_T_*` retirados como sustituidos por el sub-protocolo `TUN_OP_*` (cierra C4); los defines se quedan, no se reusan.
- [ ] **No tocar** el ring de 16 segmentos (bitmap `uint16` en `OP_LIVE_HEARTBEAT`). v8.1 no lo necesita; ensancharlo sería ruptura de wire.
- [ ] Mantener `OP_LIVE_CHUNK` / `OP_LIVE_CHUNK_V2` directos como fallback cuando el peer no tiene capacidad de tunnel o no hay circuito Active (`ShouldRouteThroughTunnel` ya implica "tunnel-or-fallback").
- [ ] Wire Kad nativo **sin cambios**: el exit habla Kad vanilla contra la DHT → nodos 0.70b no se enteran.
- [ ] Claves INI nuevas leídas con default (D2): config vieja carga limpia.
- [ ] `subscriptions.dat`: si cambia el esquema, **byte de versión** al inicio.
- [ ] Endpoints REST: el matching usa `sURL.Left(N)` — cuidado con colisiones de prefijo al añadir rutas.
- [ ] La firma Ed25519 de chunks no cambia: `OP_LIVE_T_CHUNK` envuelve un `CHUNK_V2` entero → el chunk tunelizado sigue autenticado contra la pubkey pinada.

---

## 8. Modelo de privacidad (honestidad ante todo)

Qué protege **realmente** cada modo. Importa decirlo claro en la UI y en las release notes.

| | Directo | Tunelizado (v8.1 default) | Máxima privacidad (v8.1.1+) |
|---|---|---|---|
| Búsqueda Kad oculta a observadores de red / directorio | ✗ | ✓ | ✓ |
| El emisor no aprende la identidad del espectador al suscribirse | ✗ | ✓ | ✓ |
| El emisor no aprende la IP del espectador al servir chunks | ✗ | **✗** (chunks directos) | ✓ |
| Velocidad de vídeo | nativa | nativa | **nativa − ~10-15 % de overhead** (FEC+padding); degrada adaptativamente solo si la medición lo exige (E4′) |
| Coste de ancho de banda en relays | 0 | ~0 (solo control) | ~3× unicast (v8.1.1); **→~1,3×** con dedup multicast y audiencia ≥2 (v8.1.2) |

**El matiz crítico:** `Tunelizado` oculta el **grafo social** (quién busca qué, quién se suscribe a qué canal) frente a observadores de red y el directorio Kad — pero **no oculta al espectador frente al emisor**, porque los chunks van directos y eso revela la IP del espectador al emisor en el momento de pedir el primer chunk. Para anonimato completo frente al emisor hace falta `Máxima privacidad`.

Consecuencia formal importante: la tesis (**Decisión 1.5**, garantía **G1**) exige que el plano de datos de Live vaya **siempre** por el túnel. Por tanto el modo **`Tunelizado` = solo control NO satisface G1** — un peer del mesh seguiría viendo la IP del viewer al servir chunks. Es un hito de desarrollo, no un modo de privacidad final; el estado conforme a la tesis es el túnel completo (Sprint E). Ver §12 y §13.1.

Además, recordar la limitación heredada de v8.0.0: con solo 2 nodos del fork, hop1 = hop2 = mismo nodo físico → anonimato real = 0. El anonimato real requiere **3+ nodos** del fork en la red.

---

## 9. Riesgos

| Riesgo | Mitigación |
|---|---|
| Puente del callback async de Kad (45 s) sobre un transporte pensado para 3 s — el más complejo del plan. | B2 con entrega incremental + sweeper A5; ventana de timeout ampliada y configurable. |
| Tunelizar vídeo "funciona" en loopback pero es inusable en red real (6 Mbit/s agregados). | Default control-plane-only hasta v8.1.1; FEC+multipath (S-OPT-28) en vez de cap de bitrate; adaptación medida E4′ con política D3; gate F5-E con KPIs duros (2 Mbit/s × 30 min sin stalls en 3 nodos reales) antes de prometer. La escala real la cierran dedup + viewer-relays en v8.1.2 (S-OPT-29/30). |
| Un circuito que rota o muere mid-mensaje corrompe el reassembly. | A4 (pinning + retry); E2 (sin rotación en circuitos bulk, rotar en frontera de segmento). |
| Estimación del roadmap ("v8.1 = 1-2 semanas") es optimista — ver §11. | Entregar A+B+C+D+F como v8.1; E como v8.1.x. |
| Regresión: romper interop con vanilla o con v8.0.0. | Sprint F2 como gate de release bloqueante; negociación A7. |
| El keyword sigue llegando en claro al exit. | Aceptable en el modelo de 2 saltos; documentarlo. Cifrado del keyword → roadmap v8.x/v9.0. |

---

## 10. Criterios de aceptación

v8.1 se considera lista cuando:
1. Una búsqueda Kad por keyword en modo `Tunelizado` devuelve **resultados reales de la DHT** (con filehash, IP/puerto, tags), verificada entre 2 PCs reales — y el nodo de destino no ve quién preguntó.
2. Un `subscribe` LiveTV en modo `Tunelizado` llega al broadcaster **desde el exit**, no desde el viewer; el stream arranca correctamente.
3. El modo de privacidad **persiste** entre reinicios y es seleccionable desde la UI MFC.
4. La política de fallback (STRICT/BALANCED/BEST_EFFORT) tiene efecto observable.
5. El PST pool se llena y `Acquire()` nunca devuelve null en estado estable.
6. **Gate F2:** un eMule 0.70b vanilla y un fork v8.0.0 siguen interoperando sin errores con un nodo v8.1.
7. **Tabla F5:** latencia de búsqueda tunelizada vs directa medida y publicada en las release notes (honestidad sobre el coste).
8. (v8.1.1 / E-α — gate F5-E del breakdown) un stream de ~2 Mbit/s se reproduce end-to-end en modo `Máxima privacidad` **a bitrate nativo** 30 min sin stalls entre 3 nodos reales; overhead medido en el path del viewer ≤1,20×; TTFF <8 s; matar un relay mid-stream no produce stall; el broadcaster nunca ve la IP del viewer (verificado con captura).

---

## 11. Estimación de esfuerzo (honesta)

| Bloque | Tamaño agregado | Estimación 1 dev |
|---|---|---|
| Sprint A (transporte) | 2L + 4M + 2S | ~2 semanas |
| Sprint B (Kad search) | 2L + 4M + 1S | ~1.5-2 semanas |
| Sprint C (subscribe/control + mesh) | 3L + 3M + 1S | ~2-2.5 semanas |
| Sprint D (modo) | 6M + 2S | ~1-1.5 semanas |
| Sprint F (testing) | 5M | ~1 semana |
| **Núcleo v8.1 (A+B+C+D+F)** | | **~8-9 semanas** |
| Sprint E-α → **v8.1.1** (chunks a bitrate nativo) | 3L + 3M + 1S | ~3 semanas |
| Sprint E-β → **v8.1.2** (dedup + viewer-relays) | 1L + 3M | ~2 semanas |

> El roadmap original estimaba "v8.1 = 1-2 semanas". Esa cifra **es optimista**: no contaba con que el transporte no sabe hacer multi-cell, que no hay API async y que el exit no ejecuta búsquedas reales. El núcleo honesto es ~8-9 semanas para un dev. (Se evaluó un recorte a "v8.1-mínimo" = solo A+B; **descartado** en §12.1: v8.1 entrega el núcleo A-D+F completo y el Sprint E va después: E-α en v8.1.1, E-β en v8.1.2 — §12.4.)

---

## 12. Decisiones

### 12.1 Alcance del plano de datos — DECIDIDO (2026-05-20)

**Entrega escalonada.** La tesis (**Decisión 1.5**) es tajante: el túnel de Live es always-on y **G1 no se cumple mientras los chunks vayan directos**. Decisión tomada:

- **v8.1 = núcleo A-D+F** — transporte tunelizado genérico + búsqueda Kad anónima + subscribe/control LiveTV anónimo + plumbing del modo + tests. ~8-9 semanas.
- **v8.1.1 = Sprint E-α** — data plane (chunks) por el túnel **a bitrate nativo** (re-spec §12.4); **cierra G1**. Comprometido como la release **inmediatamente siguiente**, ~3 semanas. No "deslizable".
- **v8.1 NO se anuncia como "Live privado"** y la UI no debe permitir que el usuario lo crea: con los chunks directos, ver el stream sigue exponiendo la IP del viewer — y eso es **peor que Directo**, porque el usuario se confía. Etiquetado honesto obligatorio (cf. "honest GUI text"); las operaciones Live no tunelizadas se rotulan como tales hasta v8.1.1.

**Roadmap resultante: v8.1 → v8.1.1 (Sprint E-α) → v8.1.2 (Sprint E-β) → v8.2 → v8.x → v9.0.**

### 12.2 `full ntor` — recomendado fuera de v8.1
El roadmap escrito lo situaba en "v8.x"; la sesión `focused-bohr` lo metía en v8.1. Es un cambio de cripto independiente del data plane → mantener en **v8.x**, no en v8.1/v8.1.1.

### 12.3 Cifrado del keyword hacia el exit — aceptable para v8.1
Hoy el keyword llega en claro al exit. Aceptable bajo el modelo de 2 saltos; documentado. La mejora (cifrar el keyword) → v9.0.

### 12.4 Sprint E re-especificado: bitrate nativo, sin cap — DECIDIDO (2026-06-11)

La especificación original de Sprint E compraba estabilidad **degradando el bitrate** (cap fijo 600-800 kbps, E4) porque el camino único de celdas de 512 B era frágil. Eso mezclaba dos magnitudes: el **coste agregado** (~3×, lo pagan los relays) y el **bitrate percibido** (que no tiene por qué dividirse por 3). Decisión, tras el análisis de [`V8.1.1_SPRINT_E_SPEED.md`](V8.1.1_SPRINT_E_SPEED.md):

- **La estabilidad se compra con redundancia, no degradando:** FEC Reed-Solomon adaptativo + striping multipath entre circuitos bulk (E8 + E3). Presupuesto de overhead en el path del viewer: **~10-15 %** (paridad 6-12 % adaptativa + padding ~1-2 %), en vez de recortar la calidad un 60-70 %.
- **E4 se sustituye por E4′:** entrega adaptativa dirigida por goodput **medido** (EWMA por circuito); se adapta la paridad r y el reparto de stripes, nunca el bitrate de la fuente; ante shortfall real decide la política D3 con etiquetado honesto. La constante 600-800 desaparece del plan y del código.
- **Entrega en dos fases:** **E-α = v8.1.1** (bitrate nativo por el túnel, cierra G1, ~3 semanas) y **E-β = v8.1.2** (escala: dedup multicast por subárbol E11 + viewer-as-relay E12 + split de cripto E9 + uTP condicional E10 → coste agregado ~1,3× por viewer con audiencia, ~2 semanas). Cada fase es útil por sí sola; no hacer big-bang.
- **Wire:** la celda bulk es `0xD9` (repurposing del stub `OP_LIVE_T_CHUNK`; el rango `0xD0-0xDF` está completo) con cap bit `ESE_CAP_TUNNEL_BULK 0x2000` y fallback per-symbol multi-cell. Las celdas se direccionan por contenido (`seq + symbol_idx`) **desde E-α** para que la dedup de E-β no toque el wire.
- **Descartadas con motivo** (no reabrir): 1 salto para datos (el relay único vería viewer+broadcaster → G1 probabilística) y deniability mesh (membership por IP cosechable, prohibido por §13.1).

Desglose ejecutable: [`V8.1.1_SPRINT_E_BREAKDOWN.md`](V8.1.1_SPRINT_E_BREAKDOWN.md).

## 13. Integración: bug del micro-corte y revisión de diseño externo

Consolida dos hilos paralelos para que no se reabran propuestas ya descartadas.

### 13.1 El micro-corte del mesh = el re-indexado de C6

**Síntoma:** un espectador es expulsado del mesh y sufre un micro-corte de vídeo sin causa de red.

**Causa raíz (verificada):** `CLiveMeshManager` indexa todo por puntero `CUpDownClient*` crudo — `m_meshPeers` (`LiveMeshManager.h:102`) y `m_uploadTracker` (`LiveMeshManager.h:108`). El puntero es una identidad **inestable**: cuando eMule destruye y recrea el objeto `CUpDownClient` de un peer (reconexión, churn de objetos — cf. el fix `~CUpDownClient → OnPeerDisconnected` de v7.1.8), el mesh ve un `Remove` + `Add` y **pierde el estado por-peer** (bitmap, ratio, requests pendientes). Ese reset es el micro-corte.

**Solución (tesis §5.6.3 "Mesh sobre tunnels"):** la identidad del peer pasa a ser un `TunnelEndpoint` opaco = `circ_id` + clave de circuito. El `circ_id` es estable y no cambia al intercambiarse el objeto `CUpDownClient`. Re-indexar el mesh por `TunnelEndpoint` (tareas **A8 + C6**) **arregla el micro-corte y entrega G1 con un solo cambio.**

**Prohibido:** indexar el mesh por IP. La tesis (AS-1, G1, Decisión 1.5) lo veta — una tabla de peers por IP es literalmente un registro de IPs de espectadores en memoria, justo el activo que el adversario A1 (bufete anti-piratería) quiere harvestear. NO es la solución al micro-corte, y NO se debe hacer un hotfix v8.0.28 del camino directo: ese camino se jubila en v8.1.

### 13.2 Revisión de una propuesta externa (`TUN_OP_LIVE_SUBSCRIBE` por IP)

Una propuesta externa para `TUN_OP_LIVE_SUBSCRIBE` consistía en que el exit devolviera al viewer la `IP|puerto|UDP` del broadcaster, vía túnel, "solo para la fase de descubrimiento". **Rechazada:**

- Devolver una IP cruda al viewer implica que el viewer **conecta directo** después → el emisor (y cualquier peer del swarm) ve la IP real del viewer → **AS-1 comprometido, G1 roto.** Es la misma fuga de IP que ya se descartó, un paso más adelante. Además expone la IP del broadcaster (AS-2).
- Contradice la Decisión 1.5 (túnel always-on). Tuneliza la *pregunta*, no la *sesión* — equivale a un "DNS anónimo".
- En el modelo correcto el viewer **nunca necesita la IP del broadcaster**: llama a `LiveTunnel::SendThrough(endpoint, payload)` y es el **exit** quien alcanza al broadcaster.

**Diseño correcto (C1/C2):** `TUN_OP_LIVE_SUBSCRIBE` **establece una sesión tunelizada**. El exit reenvía el `OP_LIVE_SUBSCRIBE` al broadcaster en nombre del viewer; el broadcaster ve al exit; la respuesta vuelve por el túnel; el viewer es un `TunnelEndpoint`. Sin IPs crudas en ningún sentido.

> **Responsividad de UI** (`PumpMessagesIfNeeded` en arranque/parada de emisión): describe un bug real, pero es **estabilización v8.0.x**, fuera del alcance de v8.1 — tratar en un parche aparte. La propuesta revisada (deshabilitar **todos** los controles interactivos + estado `State_Starting`/`State_Stopping`, y *entonces* bombear) es un compromiso **aceptable**: elimina la reentrada *iniciada por el usuario*. Pero **no es "100 % segura"** — el pump sigue despachando `WM_TIMER` y eventos de socket, así que la lógica del core de eMule **sí reentra**; hay que asumirlo (no mutar estado sensible durante el pump). Lo robusto sigue siendo mover el trabajo bloqueante (FFmpeg, prebuffer 14 s) a un **worker thread**.

---

*Plan derivado de auditoría de código 2026-05-20. Versión etiquetada base: v0.70b-eSE8.0.27. Sprint E re-especificado 2026-06-11 (decisión §12.4; análisis en `V8.1.1_SPRINT_E_SPEED.md`, desglose en `V8.1.1_SPRINT_E_BREAKDOWN.md`). Mantener este documento sincronizado conforme avancen los sprints.*
